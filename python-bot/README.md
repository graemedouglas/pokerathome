# Template Python bot

Create a virtualenv with (on Linux):

    python3 -m venv venv
    . venv/bin/activate
    pip install -r requirements.txt

Run with:

    python3 -m pokerathomebot ws://server:port
